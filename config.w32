// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("mulq", "for mulq support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("mulq", "enable mulq support", "no");

if (PHP_MULQV != "no") {
	EXTENSION("mulq", "mulq.c");
}

